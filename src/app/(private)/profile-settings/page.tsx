import { ProfileSettings as ProfileSettingsComponent } from "@/src/components/ProfileSettings/index";

const ProfileSettings = () => {
  return <ProfileSettingsComponent />;
};

export default ProfileSettings;
