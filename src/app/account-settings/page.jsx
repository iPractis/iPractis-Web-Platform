import { AccountSettings as AccountSettingsComponent } from "@/src/components/AccountSettings/index";

const AccountSettings = () => {
  return <AccountSettingsComponent />;
};

export default AccountSettings;
